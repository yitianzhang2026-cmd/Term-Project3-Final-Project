Frontend for ContractGuardian

Setup

cd frontend
npm install
npm run dev


Notes

- Edit `src/api/api.js` baseURL if backend runs on different host/port.
- Tailwind is configured; update `tailwind.config.js` to change palette.
